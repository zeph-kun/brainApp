from django.contrib import admin
from .models import Catalogue, Formation, Section

# Register your models here.
admin.site.register(Catalogue)
admin.site.register(Formation)
admin.site.register(Section)
